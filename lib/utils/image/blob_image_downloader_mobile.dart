// This method will never be called.
// It's used as a stub to avoid dart:html error on mobile (since we can't use dart:html import on mobile)
Future<dynamic> downloadBlobImageFromUrl(String _) async => null;
